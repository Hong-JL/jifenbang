// 这是一个配置文件模板，复制为config.js后填入你的信息
const config = {
    owner: 'Hong-JL',
    repo: 'jifenbang',
    path: '积分数据.csv',
    token: 'your-github-token',
    branch: 'main'
};

export default config; 
